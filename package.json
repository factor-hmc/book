{
  "name": "factor-book",
  "version": "1.0.0",
  "description": "Book of tutorials",
  "scripts": {
    "build": "gitbook build src build && gitbook build --format json src build/json",
    "serve": "gitbook serve src build"
  },
  "repository": {
    "type": "git",
    "url": "git@github:factor-hmc/book"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gitbook-cli": "^2.3.2"
  }
}
